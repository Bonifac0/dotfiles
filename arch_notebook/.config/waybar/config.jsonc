{
  "layer": "top",
  "position": "top",
  "height": 38,
  "spacing": 8,

  "modules-left": [
    "hyprland/workspaces"
  ],

  "modules-center": [
    "clock"
  ],

  "modules-right": [
    "cpu",
    "memory",
    "network",
    "pulseaudio",
    "hyprland/language",
    "custom/battery",
    "custom/power"
  ],

  "hyprland/workspaces": {
    "format": "{icon}",
    "on-click": "activate",
    "sort-by-number": true,
    "persistent-workspaces": {
      "*": 5
    }
  },

  "clock": {
    "format": "  {:%H:%M    %d-%m-%Y}",
    "tooltip-format": "{:%A, %d %B %Y}"
  },

  "cpu": {
    "interval": 2,
    "format": "  {usage}%",
    "tooltip": true
  },

  "memory": {
    "interval": 2,
    "format": "RAM  {used:0.1f}G",
    "tooltip": true
  },

  "network": {
    "interval": 2,
    "interface": "wlp1s0", // Optional: Change to your specific interface if it doesn't auto-detect
    "format-wifi": " {bandwidthDownBits:>6}↓ {bandwidthUpBits:>6}↑",
    "format-ethernet": "󰈀 {bandwidthDownBits:>6}↓ {bandwidthUpBits:>6}↑",
    "format-linked": "󰈀  {ifname} (No IP)",
    "format-disconnected": "󰤮 Offline",
    "tooltip-format": "{ifname} via {gwaddr} ",
    "tooltip-format-wifi": "   {essid} ({signalStrength}%)",
    "tooltip-format-ethernet": "󰈀 {ifname} ",
    "tooltip-format-disconnected": "Disconnected",
    "max-length": 50
  },

  "pulseaudio": {
    "format": "{icon}  {volume}%",
    "format-muted": "󰝟  Muted",
    "format-icons": {
      "default": ["", "", ""]
    },
    "on-click": "~/.config/waybar/scripts/toggle-pavucontrol",
    "on-scroll-up": "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+",
    "on-scroll-down": "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-",
    "tooltip": false
  },

  "hyprland/language": {
    "format": "{short}",
    "on-click": "~/.config/waybar/scripts/toggle-layout.sh",
    "tooltip": true
  },

  "custom/battery": {
    "exec": "~/.config/waybar/scripts/battery.sh",
    "interval": 30,
    "format": "{text}",
    "tooltip": true,
    "on-click": "~/.config/waybar/scripts/powerplan.sh",
    "return-type": "json"
  },

  "custom/power": {
    "format": "",
    "on-click": "~/.config/waybar/scripts/power-menu.sh",
    "tooltip": true,
    "tooltip-format": "Power options"
  }
}
}

